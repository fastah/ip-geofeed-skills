#!/usr/bin/env bash
set -euo pipefail

for required_command in go python3; do
  if ! command -v "$required_command" >/dev/null 2>&1; then
    echo "Missing required command after orb resume: $required_command" >&2
    exit 1
  fi
done

echo "Orb prerequisites are ready; no services need to be resumed."
